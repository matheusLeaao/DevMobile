import 'package:flutter/material.dart';

// Dart code to show a console message
void main() => runApp(
  Column(
    children: <Widget> [
      Text("Olá mundo!", textDirection: TextDirection.ltr),
      Text("Sejam bem-vindos", textDirection: TextDirection.ltr),
    ]
  )
);